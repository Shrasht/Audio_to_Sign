# Core modules
streamlit
numpy
soundfile
pyaudio
matplotlib
whisper
scipy
PyWavelets

# Whisper requires Torch backend
torch
torchaudio

# Optional: system-level dependencies may also be needed on some platforms:
# e.g., ffmpeg, portaudio, etc. (Mentioned here for user awareness, not needed in this file)

# If using specific versions, pin them like:
# streamlit==1.32.0
# whisper==1.1.10
